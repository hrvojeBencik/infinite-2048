---
phase: 01-performance-audit
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-performance-audit/PERF-BASELINE.md
autonomous: false
requirements: [PERF-01, PERF-05]

must_haves:
  truths:
    - "A PERF-BASELINE.md report exists with documented frame timing measurements from a physical mid-range Android device in --profile mode"
    - "Jank sources are identified from real profiling data, not hypothetical"
    - "SoundService is documented as haptic-only with PERF-05 marked N/A"
    - "Recommended Phase 2 fixes are listed with severity rankings"
  artifacts:
    - path: ".planning/phases/01-performance-audit/PERF-BASELINE.md"
      provides: "Performance baseline report with profiling measurements and jank source analysis"
      contains: "## Baseline Frame Times"
      min_lines: 80
  key_links:
    - from: ".planning/phases/01-performance-audit/PERF-BASELINE.md"
      to: "lib/core/theme/tile_themes.dart"
      via: "Documents TileThemes._activeTheme() as jank source with measured impact"
      pattern: "TileThemes"
    - from: ".planning/phases/01-performance-audit/PERF-BASELINE.md"
      to: "lib/features/game/presentation/widgets/tile_widget.dart"
      via: "Documents TileWidget rebuild frequency from profiling"
      pattern: "TileWidget"
---

<objective>
Profile the app on a physical mid-range Android device in --profile mode, document the performance baseline and real jank sources, audit SoundService (confirm N/A), and produce the PERF-BASELINE.md report.

Purpose: Establish a data-driven performance baseline so Phase 2 fixes target real jank, not hypothetical issues.
Output: PERF-BASELINE.md in the phase directory with measurements, identified jank sources, severity rankings, and Phase 2 recommendations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-performance-audit/01-CONTEXT.md
@.planning/phases/01-performance-audit/01-RESEARCH.md
@.planning/phases/01-performance-audit/01-01-SUMMARY.md

<interfaces>
<!-- Key files to audit during profiling -->

From lib/core/theme/tile_themes.dart — confirmed jank source:
- `_activeTheme()` reads Hive box + runs regex per call
- Called from `tileColor()`, `tileTextColor()` — invoked per tile per frame
- Expected: 5 Hive reads + 5 regex parses per tile per frame (4,800 regex ops/sec at 60fps on 4x4 board)

From lib/features/game/presentation/widgets/tile_widget.dart:
- 3 AnimationControllers (merge, spawn, glow)
- `_tileDecoration()` called twice in `_buildTile()` (lines 165-166)
- Each `_tileDecoration()` call invokes `TileThemes.tileColor()`

From lib/core/services/sound_service.dart — PERF-05 audit target:
- Haptic-only placeholder, no AudioPlayer instances (confirmed in CONTEXT.md D-07)
- PERF-05 is resolved N/A

Jank severity ranking methodology (from RESEARCH.md):
| Severity | Criterion |
|----------|-----------|
| P0 — Critical | >50% frames exceed 16ms during normal play |
| P1 — High | 10-50% frames exceed 16ms |
| P2 — Medium | 1-10% frames exceed 16ms |
| P3 — Low | <1% exceed 16ms |
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run profiling session on physical Android device</name>
  <files>n/a — human profiling on physical device</files>
  <read_first>
    - .planning/phases/01-performance-audit/01-RESEARCH.md (profiling methodology, pitfalls)
    - lib/core/theme/tile_themes.dart (confirm _activeTheme pattern before profiling)
    - lib/core/services/sound_service.dart (confirm haptic-only for PERF-05 audit)
    - lib/features/game/presentation/widgets/tile_widget.dart (understand TileWidget rebuild pattern to know what to look for in profiler)
  </read_first>
  <action>
This is a human checkpoint. The executor pauses and presents the verification steps below to the user. The user must connect a physical Android device and run the profiling session manually, then report back with measurements.

Before pausing, the executor should confirm the dev tools from Plan 01 are working by checking that `lib/app/dev_flags.dart` exists and `lib/features/dev/presentation/pages/dev_options_page.dart` contains `_buildPerformanceSection`.
  </action>
  <what-built>
Dev tools from Plan 01 are wired up. Now we need a physical mid-range Android device connected to run profiling.

**Mid-range device criteria (per D-02):**
- Snapdragon 7-series or equivalent (Snapdragon 695, 778G, 7 Gen 1) OR MediaTek Dimensity 700-1000 series
- A14-era Apple chip equivalent performance class
- 4-6 GB RAM
- Released 2021 or later
  </what-built>
  <how-to-verify>
1. Connect a physical Android device that matches the mid-range criteria above
2. Run `flutter devices` to confirm it is detected
3. Run `flutter run --profile` to launch the app in profile mode
4. Navigate to a level and play 10+ swipes — observe the PerformanceOverlay bars (toggle it from Dev Options > PERFORMANCE)
5. Open Flutter DevTools Performance tab (auto-opens or visit the URL printed in terminal)
6. Record a trace: press Record in DevTools, play 10 swipes, then Stop
7. Note the average frame time and any frames over 16ms from the flame chart
8. Enable "Highlight Repaints" in DevTools to check if score/HUD repaints on every swipe
9. Run the Regression Check from Dev Options and note the pass/fail result
10. Report back: device model, frame timing results (avg ms, worst ms, % over 16ms), repaint observations, regression check result

Type the results here — I will write the PERF-BASELINE.md report from your measurements.
  </how-to-verify>
  <verify>User provides profiling measurements from a physical Android device in --profile mode</verify>
  <done>User has reported: device model, average frame time, worst frame time, % over 16ms, repaint observations, and regression check result</done>
  <resume-signal>Provide: device model, average frame time, worst frame time, % frames over 16ms, whether HUD/score repaints on swipe, regression check pass/fail result, and any other observations</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write PERF-BASELINE.md report from profiling results</name>
  <files>.planning/phases/01-performance-audit/PERF-BASELINE.md</files>
  <read_first>
    - lib/core/theme/tile_themes.dart (document the _activeTheme jank source with code evidence)
    - lib/core/services/sound_service.dart (document PERF-05 N/A status)
    - lib/features/game/presentation/widgets/tile_widget.dart (document TileWidget rebuild frequency)
    - .planning/phases/01-performance-audit/01-RESEARCH.md (severity ranking methodology, TileThemes call count analysis)
  </read_first>
  <action>
**Create `.planning/phases/01-performance-audit/PERF-BASELINE.md`** with the following structure, incorporating the real measurements from the human profiling session (Task 1 results):

```markdown
# Performance Baseline Report

**Date:** {date}
**Device:** {model from Task 1 results}
**Build mode:** --profile (AOT-compiled)
**Flutter version:** 3.41.5 / Dart 3.11.3

## Baseline Frame Times

| Metric | Value |
|--------|-------|
| Average frame time | {from Task 1}ms |
| Worst frame time | {from Task 1}ms |
| Frames over 16ms | {from Task 1}% |
| Regression check | {PASS/FAIL from Task 1} |
| Total frames measured | {from Task 1} |

### Profiling Method

Measurements taken using `flutter run --profile` on a physical mid-range Android device.
Frame timing captured via `SchedulerBinding.addTimingsCallback` during a 20-swipe
automated regression check sequence (100ms between swipes, 1s shader warmup).

## Identified Jank Sources

### JS-01: TileThemes._activeTheme() — Hive + Regex Per Frame (Expected: P1)

**File:** `lib/core/theme/tile_themes.dart`

**Problem:** `_activeTheme()` reads from Hive box and runs a regex parse on every call.
Called via `tileColor()` and `tileTextColor()` from `TileWidget._buildTile()`.

**Call count per tile per frame:** 5 (confirmed by code inspection)
- `_tileDecoration()` call 1 (line ~165) → `tileColor()` → `_activeTheme()`
- `_tileDecoration()` call 2 (line ~166, `.copyWith`) → `tileColor()` → `_activeTheme()`
- `_getTileMainColor()` → `tileColor()` → `_activeTheme()`
- `_valueContent()` → `tileTextColor()` → `_activeTheme()`
- Additional call from gradient construction

**Impact:** 16 tiles x 5 calls x 60fps = **4,800 regex operations/second** on the UI thread.

**Measured severity:** {P0/P1/P2/P3 based on Task 1 data}

**Recommended fix (Phase 2):** Refactor TileThemes to reactive theme passed via ProgressionBloc state (PERF-02). Cache the active theme once and pass it down — eliminates all per-frame Hive reads and regex parses.

### JS-02: No RepaintBoundary Usage (Expected: P2)

**Problem:** Zero `RepaintBoundary` widgets in the codebase. Game board, score display,
and controls all repaint together on any state change.

**Observed behavior:** {from Task 1 — did HUD/score repaint on every swipe?}

**Measured severity:** {P0/P1/P2/P3 based on repaint observation}

**Recommended fix (Phase 2):** Add RepaintBoundary to isolate game board from score/HUD (PERF-03).

### JS-03: TileWidget Triple AnimationController (Expected: P3)

**Problem:** Each TileWidget creates 3 AnimationControllers (merge, spawn, glow).
AnimatedBuilder listens to merged Listenable — overlapping controller ticks can
cause redundant builds.

**Measured severity:** {P3 expected — profiling may not show this as significant}

**Recommended fix (Phase 2):** Monitor during Phase 2 — fix only if profiling confirms impact.

## SoundService Audit (PERF-05)

**Status:** RESOLVED — N/A

**Finding:** `lib/core/services/sound_service.dart` is a haptic-only placeholder. No `AudioPlayer`
instances exist. No `audioplayers` package import beyond what is declared in pubspec.yaml.
The `SoundService` class delegates to `HapticFeedback` from Flutter SDK — there is no
audio playback and therefore no memory leak risk from AudioPlayer pooling.

**Action:** None required. If real audio playback is added in a future milestone, re-audit
for AudioPlayer lifecycle management at that time.

## Recommended Phase 2 Fix Priority

| Priority | ID | Fix | Requirement |
|----------|-----|-----|-------------|
| 1 | JS-01 | TileThemes reactive refactor | PERF-02 |
| 2 | JS-02 | RepaintBoundary isolation | PERF-03 |
| 3 | — | BLoC buildWhen guards | PERF-04 |

## Raw Observations

{Any additional notes from the profiling session — flame chart observations,
specific widget rebuild patterns, GPU vs UI thread breakdown, etc.}
```

Fill in all `{placeholder}` values from the Task 1 checkpoint results. Use the actual measurements — do not invent data. If the profiling showed unexpected results (e.g., TileThemes is NOT a significant jank source), document that honestly and adjust the severity ranking and Phase 2 priority accordingly.
  </action>
  <verify>
    <automated>test -f /home/hrxydark/Projects/Infinite2048/infinite-2048/.planning/phases/01-performance-audit/PERF-BASELINE.md && grep -c "##" /home/hrxydark/Projects/Infinite2048/infinite-2048/.planning/phases/01-performance-audit/PERF-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/01-performance-audit/PERF-BASELINE.md exists
    - PERF-BASELINE.md contains `## Baseline Frame Times` with a metrics table
    - PERF-BASELINE.md contains `--profile` (documenting build mode)
    - PERF-BASELINE.md contains `## Identified Jank Sources`
    - PERF-BASELINE.md contains `TileThemes._activeTheme()` as a documented jank source
    - PERF-BASELINE.md contains `RepaintBoundary` as a documented concern
    - PERF-BASELINE.md contains `## SoundService Audit` with `RESOLVED` or `N/A`
    - PERF-BASELINE.md contains `## Recommended Phase 2 Fix Priority`
    - PERF-BASELINE.md contains actual device model name (not a placeholder)
    - PERF-BASELINE.md contains actual frame timing numbers (not placeholders)
    - PERF-BASELINE.md has at least 80 lines
  </acceptance_criteria>
  <done>PERF-BASELINE.md report is complete with real profiling data from a physical device, identified jank sources with severity rankings, SoundService audit marked N/A, and Phase 2 fix recommendations prioritized.</done>
</task>

</tasks>

<verification>
1. PERF-BASELINE.md exists in `.planning/phases/01-performance-audit/`
2. Report contains real measurements from a physical device (not estimates or hypothetical)
3. All jank sources have severity rankings based on profiling data
4. SoundService audit section confirms PERF-05 is resolved N/A
5. Phase 2 recommendations are prioritized by measured impact
</verification>

<success_criteria>
- PERF-01: Frame timing baseline is documented from a profiling session on a mid-range Android device in --profile mode
- PERF-05: SoundService audited and confirmed as haptic-only — no AudioPlayer leak risk. Marked N/A in report.
- PERF-BASELINE.md is complete, structured, and contains real data — not hypothetical
</success_criteria>

<output>
After completion, create `.planning/phases/01-performance-audit/01-02-SUMMARY.md`
</output>
