---
phase: 05-store-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - ios/Runner/PrivacyInfo.xcprivacy
autonomous: true
requirements:
  - STORE-01
  - STORE-03
  - STORE-06

must_haves:
  truths:
    - "App icon is configured with adaptive icon background and foreground for Android"
    - "iOS icon generation includes remove_alpha_ios: true"
    - "PrivacyInfo.xcprivacy declares UserDefaults, FileTimestamp, DiskSpace, and SystemBootTime APIs"
    - "Privacy policy URL is live and returns HTTP 200"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Complete flutter_launcher_icons config with adaptive icon fields"
      contains: "adaptive_icon_background"
    - path: "ios/Runner/PrivacyInfo.xcprivacy"
      provides: "Apple privacy manifest with required API declarations"
      contains: "NSPrivacyAccessedAPICategoryUserDefaults"
  key_links:
    - from: "pubspec.yaml"
      to: "assets/icon/icon.png"
      via: "flutter_launcher_icons image_path and adaptive_icon_foreground"
      pattern: "image_path.*assets/icon/icon.png"
---

<objective>
Configure app icon generation for both platforms and create iOS privacy manifest.

Purpose: STORE-01 (icon compliance) and STORE-06 (privacy manifest) are blockers for any store submission. These are pure config tasks with no code dependencies.
Output: Updated pubspec.yaml with complete icon config, new PrivacyInfo.xcprivacy file, verified privacy policy URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-store-preparation/05-CONTEXT.md
@.planning/phases/05-store-preparation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update flutter_launcher_icons config and generate icons</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml (current flutter_launcher_icons section at bottom of file, lines 53-56)
    - lib/core/theme/app_colors.dart (AppColors.background = Color(0xFF0A0E21) on line 6)
    - assets/icon/icon.png (verify it exists)
  </read_first>
  <action>
Update the `flutter_launcher_icons:` section in pubspec.yaml (lines 53-56) to add adaptive icon config and iOS alpha removal. Replace the existing section with:

```yaml
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/icon/icon.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#0A0E21"
  adaptive_icon_foreground: "assets/icon/icon.png"
```

Per D-01: uses user-provided source icon.
Per D-02: adaptive icon background uses AppColors.background (#0A0E21 — deep navy).
Per D-03: remove_alpha_ios: true ensures no alpha channel on iOS.

After updating pubspec.yaml, run the icon generator:
```bash
dart run flutter_launcher_icons
```

Verify output files exist:
- `android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png` (adaptive foreground generated)
- `android/app/src/main/res/mipmap-hdpi/ic_launcher.png` (legacy icon)
  </action>
  <verify>
    <automated>grep -q "adaptive_icon_background" pubspec.yaml && grep -q "remove_alpha_ios: true" pubspec.yaml && test -f android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `adaptive_icon_background: "#0A0E21"`
    - pubspec.yaml contains `adaptive_icon_foreground: "assets/icon/icon.png"`
    - pubspec.yaml contains `remove_alpha_ios: true`
    - File `android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png` exists
    - File `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_foreground.png` exists
    - `dart run flutter_launcher_icons` exits with code 0
  </acceptance_criteria>
  <done>flutter_launcher_icons config includes adaptive icon background (#0A0E21), adaptive foreground (icon.png), and remove_alpha_ios: true. Icon generator ran successfully and produced foreground PNGs in all mipmap densities.</done>
</task>

<task type="auto">
  <name>Task 2: Create iOS PrivacyInfo.xcprivacy and verify privacy policy URL</name>
  <files>ios/Runner/PrivacyInfo.xcprivacy</files>
  <read_first>
    - lib/core/services/remote_config_service.dart (privacyPolicyUrl getter, line 39-41, default URL on line 10)
    - ios/Runner/ (directory listing to confirm file does not exist yet)
  </read_first>
  <action>
Create `ios/Runner/PrivacyInfo.xcprivacy` with the following exact content — this declares required-reason APIs used by Flutter engine + Hive + Firebase:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>NSPrivacyAccessedAPITypes</key>
  <array>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>CA92.1</string>
      </array>
    </dict>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryFileTimestamp</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>3B52.1</string>
      </array>
    </dict>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryDiskSpace</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>7D9E.1</string>
      </array>
    </dict>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategorySystemBootTime</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>35F9.1</string>
      </array>
    </dict>
  </array>
  <key>NSPrivacyCollectedDataTypes</key>
  <array/>
  <key>NSPrivacyTracking</key>
  <false/>
  <key>NSPrivacyTrackingDomains</key>
  <array/>
</dict>
</plist>
```

Per D-13: No ATT prompt in app, so NSPrivacyTracking is false. NSPrivacyCollectedDataTypes is empty because Firebase/AdMob SDKs bundle their own privacy manifests declaring their collected types.

After creating the file, verify the privacy policy URL is accessible:
```bash
curl -s -o /dev/null -w "%{http_code}" "https://hrvojebencik.github.io/infinite-2048/privacy-policy.html"
```
Expected: 200. Per D-12, this URL is already live.

STORE-03 is verified by confirming both the URL returns 200 AND RemoteConfigService._defaultPrivacyPolicy matches this URL exactly.
  </action>
  <verify>
    <automated>test -f ios/Runner/PrivacyInfo.xcprivacy && grep -q "NSPrivacyAccessedAPICategoryUserDefaults" ios/Runner/PrivacyInfo.xcprivacy && grep -q "NSPrivacyAccessedAPICategoryFileTimestamp" ios/Runner/PrivacyInfo.xcprivacy && grep -q "NSPrivacyAccessedAPICategoryDiskSpace" ios/Runner/PrivacyInfo.xcprivacy && grep -q "NSPrivacyAccessedAPICategorySystemBootTime" ios/Runner/PrivacyInfo.xcprivacy && grep -q "NSPrivacyTracking" ios/Runner/PrivacyInfo.xcprivacy && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `ios/Runner/PrivacyInfo.xcprivacy` exists
    - File contains `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`
    - File contains `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `3B52.1`
    - File contains `NSPrivacyAccessedAPICategoryDiskSpace` with reason `7D9E.1`
    - File contains `NSPrivacyAccessedAPICategorySystemBootTime` with reason `35F9.1`
    - File contains `<key>NSPrivacyTracking</key>` followed by `<false/>`
    - Privacy policy URL returns HTTP 200
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy created in ios/Runner/ with all four required-reason API declarations. Privacy policy URL confirmed accessible. STORE-03 and STORE-06 satisfied.</done>
</task>

</tasks>

<verification>
- `grep -c "adaptive_icon" pubspec.yaml` returns 2 (background + foreground)
- `test -f ios/Runner/PrivacyInfo.xcprivacy` exits 0
- `curl -s -o /dev/null -w "%{http_code}" "https://hrvojebencik.github.io/infinite-2048/privacy-policy.html"` returns 200
- `flutter analyze` passes (no new code introduced, only config)
</verification>

<success_criteria>
- App icon generation produces adaptive icon assets for Android (foreground PNGs in all mipmap dirs)
- iOS icon removal of alpha channel is configured
- PrivacyInfo.xcprivacy exists with 4 API category declarations
- Privacy policy URL is live
</success_criteria>

<output>
After completion, create `.planning/phases/05-store-preparation/05-01-SUMMARY.md`
</output>
