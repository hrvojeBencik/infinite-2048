---
phase: 05-store-preparation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-store-preparation/STORE_LISTING.md
  - pubspec.yaml
autonomous: true
requirements:
  - STORE-04
  - STORE-05
  - STORE-09

must_haves:
  truths:
    - "App Store listing has title, subtitle, keywords, full description, and category"
    - "Play Store listing has title, short description, full description, and category"
    - "Store copy uses ASO-optimized keywords targeting puzzle/2048 genre"
    - "App name is '2048: Merge Quest' on both platforms"
    - "Version bumped from 1.0.0+1 to release-ready version"
  artifacts:
    - path: ".planning/phases/05-store-preparation/STORE_LISTING.md"
      provides: "Complete ASO-optimized store listings for both platforms"
      contains: "2048: Merge Quest"
    - path: "pubspec.yaml"
      provides: "Version bumped for release"
      contains: "version: 1.0.0+2"
  key_links: []
---

<objective>
Generate ASO-optimized store metadata for both App Store and Google Play, and bump the app version for release.

Purpose: STORE-04, STORE-05, and STORE-09 require complete store listings with keyword-optimized copy. The metadata is platform text — no code changes beyond version bump.
Output: STORE_LISTING.md with complete App Store and Google Play metadata, version bumped in pubspec.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-store-preparation/05-CONTEXT.md
@.planning/phases/05-store-preparation/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate ASO-optimized store listings for both platforms</name>
  <files>.planning/phases/05-store-preparation/STORE_LISTING.md</files>
  <read_first>
    - .planning/phases/05-store-preparation/05-CONTEXT.md (D-08: app name, D-09: tone, D-10: category, D-11: use /aso-expert)
    - .planning/phases/05-store-preparation/05-UI-SPEC.md (copywriting contract lines 159-182 — title, subtitle, short description)
    - lib/core/constants/app_constants.dart (appName = '2048: Merge Quest')
    - lib/features/levels/data/datasources/levels_local_datasource.dart (discover zone/level count for feature claims)
    - lib/features/game/domain/entities/power_up_type.dart (power-up types for feature list)
    - lib/features/achievements/domain/entities/ (achievement types for feature claims)
  </read_first>
  <action>
Per D-11, use the `/aso-expert` skill if available. If the skill is not available, generate the store listings manually following ASO best practices.

Create `.planning/phases/05-store-preparation/STORE_LISTING.md` with the following structure:

**App Store (iOS) — STORE-04:**
- **Title:** "2048: Merge Quest" (per D-08, 16 chars — under 30 char limit)
- **Subtitle:** "Puzzle Game with Levels & Zones" (per UI-SPEC copywriting contract, under 30 chars)
- **Keywords:** Comma-separated, 100 char max. Target: puzzle, merge, brain, number, tile, strategy, logic, casual, challenge, daily. Exclude "2048" and "game" (already in title/subtitle). No spaces after commas.
- **Category:** Games > Puzzle (per D-10)
- **Description:** 4000 char max. Structure: hook paragraph (what makes it unique), feature bullets, social proof/call-to-action. Tone: casual, playful, emoji-friendly per D-09.
- **Promotional Text:** 170 char max. Updated without new submission — use for seasonal/event callouts.

**Google Play — STORE-05:**
- **Title:** "2048: Merge Quest" (per D-08, under 30 chars)
- **Short Description:** "Classic 2048 puzzle with levels, zones, and daily challenges" (per UI-SPEC copywriting contract, under 80 chars)
- **Full Description:** 4000 char max. Similar structure to iOS but optimized for Play Store keyword indexing (keywords in first 2 lines, repeated naturally).
- **Category:** Game > Puzzle (per D-10)

**ASO keyword strategy notes:**
- Primary keywords: 2048, puzzle, merge, tiles, brain teaser
- Secondary keywords: levels, zones, daily challenge, achievements, number game
- Long-tail: "2048 with levels", "puzzle game offline", "brain training puzzle"
- Tone per D-09: casual & fun, playful, emoji-friendly

The listing file must include both platforms' complete metadata ready for copy-paste into App Store Connect and Google Play Console.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-store-preparation/STORE_LISTING.md && grep -q "2048: Merge Quest" .planning/phases/05-store-preparation/STORE_LISTING.md && grep -q "Keywords" .planning/phases/05-store-preparation/STORE_LISTING.md && grep -q "Short Description" .planning/phases/05-store-preparation/STORE_LISTING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-store-preparation/STORE_LISTING.md` exists
    - File contains "2048: Merge Quest" as title for both platforms
    - File contains an iOS Keywords field with comma-separated keywords (100 char max)
    - File contains iOS Subtitle under 30 characters
    - File contains Play Store Short Description under 80 characters
    - File contains iOS full Description (at least 500 characters)
    - File contains Play Store Full Description (at least 500 characters)
    - File contains "Puzzle" as category for both platforms
    - Descriptions use casual, emoji-friendly tone per D-09
  </acceptance_criteria>
  <done>STORE_LISTING.md contains complete ASO-optimized metadata for both App Store and Google Play, ready for direct copy-paste into store consoles. STORE-04, STORE-05, STORE-09 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Bump app version for release submission</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml (line 4: version: 1.0.0+1)
  </read_first>
  <action>
Update `pubspec.yaml` line 4 from:
```yaml
version: 1.0.0+1
```
to:
```yaml
version: 1.0.0+2
```

The marketing version stays 1.0.0 (first public release). The build number increments to 2 because +1 may have been used in prior test uploads. Build number must be a positive integer greater than any previously submitted build. If the user has submitted builds before, they will need to increment further — but +2 is the safe default for a first submission.
  </action>
  <verify>
    <automated>grep -q "version: 1.0.0+2" pubspec.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `version: 1.0.0+2`
    - No other version string remains as `1.0.0+1`
  </acceptance_criteria>
  <done>App version bumped to 1.0.0+2 for release submission.</done>
</task>

</tasks>

<verification>
- STORE_LISTING.md exists with both iOS and Android sections
- `grep -c "2048: Merge Quest" .planning/phases/05-store-preparation/STORE_LISTING.md` returns at least 2 (one per platform)
- `grep "version:" pubspec.yaml` shows 1.0.0+2
</verification>

<success_criteria>
- Complete store metadata for both platforms exists in STORE_LISTING.md
- App Store metadata includes title, subtitle, keywords, description, category
- Play Store metadata includes title, short description, full description, category
- Version bumped to 1.0.0+2
</success_criteria>

<output>
After completion, create `.planning/phases/05-store-preparation/05-03-SUMMARY.md`
</output>
